# Qwik City Broken Routes Lab

Link and nav to proxies do not work in production `preview`.

Old school window.location.href navigates but only works in production `preview`.

Vite proxies do not work at all in dev mode `dev`.


NOTE: Use latest qwik and qwik-city build, or local build.
